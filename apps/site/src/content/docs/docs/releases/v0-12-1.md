---
title: v0.12.1 — Apple silicon desktop repair
description: Native Apple silicon packaging, the supplied nine-diamond icon, and a branded Mac installer.
---

Dusori v0.12.1 repairs the Mac distribution that shipped with v0.12.0. The macOS app is now Apple silicon only; Windows x64 remains supported.

:::caution[Superseded by v0.12.2]
Every v0.12.0 or v0.12.1 desktop build needs one manual v0.12.2 install. Both affected versions open to a launch-time 404, so Settings is inaccessible. The Intel v0.12.0 Mac build also cannot safely select the Apple silicon updater feed. Your workspace is stored separately from the app bundle and remains in place; normal in-app updates resume from v0.12.2 onward.
:::

## Mac packaging

- The macOS executable and bundled Node.js 24 companion runtime are built for `arm64`.
- Intel Mac builds and the `darwin-x86_64` updater entry are removed.
- Finder, Applications, and the Dock use the supplied nine-diamond icon.
- The disk image uses a branded drag-to-Applications layout instead of the unstyled default window.
- Release verification rejects a Mac build unless it is the Apple silicon target.

## One-time upgrade from v0.12.0

The Intel-targeted v0.12.0 Mac app identifies itself as Intel even when Rosetta runs it on an M-series Mac. It therefore cannot safely choose this release's Apple silicon updater entry. This release originally provided the manual architecture repair, but v0.12.2 now supersedes it because v0.12.1 cannot reach the updater UI. The workspace is stored separately from the app bundle and remains in place.

## Updates and safety

- `latest.json` contains only Apple silicon macOS and Windows x64 updater entries.
- Check, download, install, and restart remain separate actions.
- The app still refuses to install an update while work is unsaved.
- The OS installers are not Apple-notarized or Microsoft code-signed. Gatekeeper or SmartScreen may warn even though in-app update artifacts use a separate Dusori signature.

## Start

- [Download current v0.12.2](https://github.com/udhawan97/Dusori/releases/tag/v0.12.2)
- [View superseded v0.12.1 release](https://github.com/udhawan97/Dusori/releases/tag/v0.12.1)
- [Read the installation guide](../../getting-started/)
- [Read the update and recovery guide](../../updates/)
